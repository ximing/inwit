# T15: 卡片详情页 + 复习页重构 + 全站视觉收尾

工作目录 /Users/ximing/project/mygithub/inwit。读 docs/design-system.md（§5、§6.3、§6.4、§7、§8）和 docs/prd.md §2.6。前置：T11–T14 完成。

## 目标 1：卡片详情页（/cards/:id）
按设计系统 §6.3：
1. 卡片本体：概念（衬线 lg 600）/例子/易混点/标签/全部自测题（含答案折叠）
2. 「出处」条：来自《文档标题》+ 锚点原文引用（斜体衬线）→ 点击跳 /doc/:id 并滚动高亮到该锚点（URL 带 ?anchor=<cardId>，阅读页支持滚动定位+闪烁一次强调）
3. 「相关卡片」：GET /api/cards/:id/links 渲染 outgoing+incoming，按 type 分组（同概念/易混淆/前置/相关），小卡横排，点击跳转；agent 建的边显示 reason，用户可删（DELETE /api/card-links/:id）
4. 复习状态小字：下次复习时间、当前间隔

## 目标 2：复习页重构（§6.4）
1. 居中窄栏 560px；抽认卡为大纸面（radius-lg + shadow-2 + 衬线）
2. 翻面：rotateY 180° 300ms（respect prefers-reduced-motion，降级为淡入）
3. **关联轻提示**：卡片顶部小字「关联 N 张卡片」，点开是底部抽屉式小列表（不打断流，不强制看）
4. 三档按钮：忘了/模糊/想起来了 按 §5 语义色描边规范；完成态页面按 §8 文案语气重写
5. 进度显示改为顶部细进度条（`--accent` 色，2px 高）+ 已刷/总数小字

## 目标 3：全站收尾
1. 线程页、设置页、后台页按设计系统过一遍：间距/字号/组件统一（后台改名为「任务与用量」，视觉降级为次要——更小的标题、更素的表格）
2. 空状态文案按 §8 全部重写（文档列表空、复习完成、无线程、无配置）
3. 检查全站无写死颜色、无旧样式残留；暗主题下所有页面过一遍
4. root README 更新新路由结构；docs/dev-log.md 追加 T11–T15 摘要

## 验收
- `pnpm -r typecheck && pnpm -r build && pnpm --filter @inwit/server test` 全绿
- 浏览器实走全链路（明暗各一遍）：文档→锚点→卡片详情→相关卡片跳转→出处回跳→复习（翻面动画、关联提示、三档反馈）→完成态
- 截图 docs/screenshots/t15-card.png / t15-review.png / t15-review-dark.png
- 报告 docs/tasks/t15-report.md
