# T16: 全量改名 thread → topic（无兼容包袱）

工作目录 /Users/ximing/project/mygithub/inwit。读 docs/prd.md §2.5（术语定稿：废弃"线程"，统一"主题"）。没有线上用户，直接改，不留兼容层。

## 目标
1. **DB**：drizzle 迁移——表 threads → topics；所有外键列 thread_id → topic_id（documents、captures 已无；memories.scope 值 'thread'→'topic'，layer 'thread_map'→'topic_map'，scope_id 不变）；jobs payload 里的 threadId 字段名（新写的 job 用 topicId，旧 payload 不用管）
2. **Server**：路由 /api/threads → /api/topics；所有 service/handler/dto 字段重命名
3. **Web**：路由 /threads → /topics；侧栏「线程」→「主题」；线程切换器 → 主题切换器；所有文案（"新建线程"→"新建主题" 等）；首页占位/空状态文案同步
4. **dto**：Thread schema → Topic
5. 全仓搜 `thread|Thread|线程`，除 node_modules/dist/docs/tasks 历史报告外应为 0（docs/prd.md 和 dev-log 保留历史叙述可以例外，代码里必须为 0）

## 验收
- migrate 跑通；psql 确认 topics 表存在、threads 不存在、memories 存量 scope 已更新
- `pnpm -r typecheck && pnpm -r build && pnpm --filter @inwit/server test` 全绿
- 浏览器实走：建主题、归档、主题切换器过滤、文档归属主题
- 报告 docs/tasks/t16-report.md
