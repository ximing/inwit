<!-- Generated by scripts/generate-inwit-skill-api.mjs. Do not edit. -->

# 管理

Generated 2026-09-20T05:12:01.638Z. 5 endpoints.

### GET `/api/admin/executions`

auth=bearer

- **query** `listAdminExecutionsQuerySchema` (dto)
```json
{
  "type": "object",
  "properties": {
    "page": {
      "type": "integer",
      "coerced": true,
      "minimum": 1,
      "optional": true,
      "default": 1
    },
    "limit": {
      "type": "integer",
      "coerced": true,
      "minimum": 1,
      "maximum": 100,
      "optional": true,
      "default": 20
    },
    "agentType": {
      "type": "string",
      "enum": [
        "digest",
        "evolve",
        "weekly_report",
        "topic",
        "chat",
        "selection",
        "extract",
        "ocr"
      ],
      "optional": true
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "running",
        "done",
        "failed"
      ],
      "optional": true
    }
  },
  "additionalProperties": false
}
```

### GET `/api/admin/executions/:id`

auth=bearer

- **params** `idParamsSchema` (local)
```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

### GET `/api/admin/jobs`

auth=bearer

- **query** `listAdminJobsQuerySchema` (dto)
```json
{
  "type": "object",
  "properties": {
    "page": {
      "type": "integer",
      "coerced": true,
      "minimum": 1,
      "optional": true,
      "default": 1
    },
    "limit": {
      "type": "integer",
      "coerced": true,
      "minimum": 1,
      "maximum": 100,
      "optional": true,
      "default": 20
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "running",
        "done",
        "failed"
      ],
      "optional": true
    },
    "type": {
      "type": "string",
      "enum": [
        "digest",
        "evolve",
        "weekly_report",
        "topic",
        "chat",
        "selection",
        "extract",
        "ocr",
        "annotation_resurface"
      ],
      "optional": true
    }
  },
  "additionalProperties": false
}
```

### POST `/api/admin/jobs/:id/retry`

auth=bearer

- **params** `idParamsSchema` (local)
```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

### GET `/api/admin/usage/summary`

auth=bearer

- **query** `usageSummaryQuerySchema` (dto)
```json
{
  "type": "object",
  "properties": {
    "days": {
      "type": "integer",
      "coerced": true,
      "minimum": 1,
      "maximum": 365,
      "optional": true,
      "default": 30
    }
  },
  "additionalProperties": false
}
```


