<!-- Generated by scripts/generate-inwit-skill-api.mjs. Do not edit. -->

# 复习

Generated 2026-09-24T08:06:29.100Z. 8 endpoints.

### POST `/api/review/:cardId/feedback`

auth=bearer

- **params** `cardIdParamsSchema` (local)
```json
{
  "type": "object",
  "properties": {
    "cardId": {
      "type": "string",
      "format": "uuid"
    }
  },
  "required": [
    "cardId"
  ],
  "additionalProperties": false
}
```

- **body** `submitReviewFeedbackSchema` (dto)
```json
{
  "type": "object",
  "properties": {
    "feedback": {
      "type": "string",
      "enum": [
        "forgot",
        "fuzzy",
        "remembered"
      ]
    }
  },
  "required": [
    "feedback"
  ],
  "additionalProperties": false
}
```

### GET `/api/review/checkins`

auth=bearer

- **query** `checkinsQuerySchema` (local)
```json
{
  "type": "object",
  "properties": {
    "month": {
      "type": "string",
      "pattern": "^\\d{4}-(0[1-9]|1[0-2])$"
    }
  },
  "required": [
    "month"
  ],
  "additionalProperties": false
}
```

### GET `/api/review/settings`

auth=bearer

### PUT `/api/review/settings`

auth=bearer

- **body** `reviewSettingsSchema` (dto)
```json
{
  "type": "object",
  "properties": {
    "dailyReviewLimit": {
      "type": "integer",
      "minimum": 5,
      "maximum": 100
    },
    "dailyNewLimit": {
      "type": "integer",
      "minimum": 0,
      "maximum": 30
    },
    "startingEase": {
      "type": "number",
      "minimum": 1.3,
      "maximum": 3
    },
    "fuzzyScale": {
      "type": "number",
      "minimum": 1,
      "maximum": 1.5
    },
    "learningSteps": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 1,
        "maximum": 30
      },
      "minItems": 1,
      "maxItems": 4,
      "refined": true
    }
  },
  "required": [
    "dailyReviewLimit",
    "dailyNewLimit",
    "startingEase",
    "fuzzyScale",
    "learningSteps"
  ],
  "additionalProperties": false
}
```

### GET `/api/review/stats`

auth=bearer

### GET `/api/review/stats/topics`

auth=bearer

### GET `/api/review/struggling`

auth=bearer

- **query** `strugglingQuerySchema` (local)
```json
{
  "type": "object",
  "properties": {
    "limit": {
      "type": "integer",
      "coerced": true,
      "minimum": 1,
      "maximum": 20,
      "optional": true
    }
  },
  "additionalProperties": false
}
```

### GET `/api/review/today`

auth=bearer


